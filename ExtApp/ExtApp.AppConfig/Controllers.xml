<objects xmlns="http://www.springframework.net">

  <!-- 登录 -->
  <object type="ExtApp.Controller.LoginController, ExtApp.Controller" singleton="false">
    <property name="bll" ref="LoginBLL" />
  </object>

  <!-- 菜单 -->
  <object type="ExtApp.Controller.MenuController, ExtApp.Controller" singleton="false">
    <property name="bll" ref="MenuBLL" />
  </object>

  <!-- 日志 -->
  <object type="ExtApp.Controller.LogController, ExtApp.Controller" singleton="false">
    <property name="bll" ref="LogBLL" />
  </object>

  <!-- 用户 -->
  <object type="ExtApp.Controller.UserController, ExtApp.Controller" singleton="false">
    <property name="bll" ref="UserBLL" />
  </object>

  <!-- 机构 -->
  <object type="ExtApp.Controller.DeptController, ExtApp.Controller" singleton="false">
    <property name="bll" ref="DeptBLL" />
  </object>

</objects>

